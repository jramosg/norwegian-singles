log_format monetization
    'monetization $time_iso8601 type=$arg_type placement=$arg_placement '
    'item=$arg_item locale=$arg_locale path=$arg_path';

server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # Performance optimizations
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;
    server_tokens off;

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_buffers 16 8k;
    gzip_http_version 1.1;
    gzip_min_length 256;
    gzip_types
        application/javascript
        application/json
        application/x-javascript
        application/xml
        text/css
        text/javascript
        text/plain
        text/xml
        image/svg+xml;

    # Security Headers
    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-XSS-Protection "1; mode=block";
    add_header X-Content-Type-Options "nosniff";

    # Root redirect to default locale (English)
    location = / {
        return 301 /en/;
    }

    # First-party, cookie-free outbound click events. These are written to
    # container stdout and can be inspected with `docker logs`.
    location = /events/outbound {
        access_log /dev/stdout monetization;
        add_header Cache-Control "no-store";
        return 204;
    }

    # Handle Astro's static routing
    location / {
        try_files $uri $uri/ $uri.html /index.html;
    }

    # Fingerprinted Astro build assets are immutable — cache forever.
    location /_astro/ {
        expires 1y;
        access_log off;
        add_header Cache-Control "public, immutable, no-transform";
    }

    # Cache static assets (highly optimized)
    location ~* \.(?:js|css|png|jpg|jpeg|gif|ico|svg|webp|avif|woff|woff2|ttf|otf)$ {
        expires 1y;
        access_log off;
        add_header Cache-Control "public, no-transform";
    }

    # Error pages
    error_page 404 /404.html;
    location = /404.html {
        internal;
    }
}
