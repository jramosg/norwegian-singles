---
import AppLayout from '../layouts/AppLayout.astro';
import TrainingForm from './react/TrainingForm';
import MethodologySection from './MethodologySection.astro';
import BookRecommendations from './BookRecommendations.astro';
import { useTranslations } from '../i18n/ui';
import { getSeoPages, getVisibleFaqs } from '../lib/seo-pages';
import { getRacePagesByDistance } from '../lib/race-seo-pages';
import type { Locale } from '../types';

interface PaceCardData {
  raceLabel: string;
  rep3min: string;
  rep6min: string;
  rep10min: string;
  easy: string;
}

interface Props {
  locale: Locale;
  title?: string;
  description?: string;
  structuredData?: Record<string, unknown>[];
  seoHeading?: string;
  seoIntro?: string;
  paceCard?: PaceCardData;
}

const {
  locale,
  title,
  description,
  structuredData,
  seoHeading,
  seoIntro,
  paceCard,
} = Astro.props;
const t = useTranslations(locale);
const relatedPages = getSeoPages(locale);
const faqs = getVisibleFaqs(locale);
const racePagesByDistance = getRacePagesByDistance(locale);
const isSeoLanding = Boolean(seoHeading);
const currentPath = Astro.url.pathname.replace(/\/$/, '');
const isCurrentHref = (href: string) =>
  href.split('#')[0].replace(/\/$/, '') === currentPath;
const raceGroups = {
  en: [
    { key: '5K' as const, label: '5K' },
    { key: '10K' as const, label: '10K' },
    { key: 'HM' as const, label: 'Half Marathon' },
    { key: 'marathon' as const, label: 'Marathon' },
  ],
  es: [
    { key: '5K' as const, label: '5K' },
    { key: '10K' as const, label: '10K' },
    { key: 'HM' as const, label: 'Media Maratón' },
    { key: 'marathon' as const, label: 'Maratón' },
  ],
  ko: [
    { key: '5K' as const, label: '5K' },
    { key: '10K' as const, label: '10K' },
    { key: 'HM' as const, label: '하프 마라톤' },
    { key: 'marathon' as const, label: '마라톤' },
  ],
}[locale];
const faqStructuredData = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: faqs.map((faq) => ({
    '@type': 'Question',
    name: faq.q,
    acceptedAnswer: {
      '@type': 'Answer',
      text: faq.a,
    },
  })),
};

const proofBullets = {
  en: [
    'Paces from 5K or 10K',
    '4.5-9h weekly plans',
    '15-week marathon build',
    'Shareable plan URL',
  ],
  es: [
    'Ritmos desde 5K o 10K',
    'Planes semanales de 4.5-9 h',
    'Preparación maratón de 15 semanas',
    'URL compartible del plan',
  ],
  ko: [
    '5K 또는 10K 기반 페이스',
    '주 4.5-9시간 플랜',
    '15주 마라톤 빌드업',
    '공유 가능한 플랜 URL',
  ],
}[locale];

const sample = {
  en: {
    eyebrow: 'Instant sample output',
    title: 'NSA / NSM Week',
    input: '20:00 5K · 6 h/week',
    days: [
      ['Tue', '3 x 10 min', '4:28/km'],
      ['Thu', '5 x 6 min', '4:15/km'],
      ['Sat', '9 x 3 min', '4:04/km'],
      ['Sun', '75 min easy', '5:35/km'],
    ],
  },
  es: {
    eyebrow: 'Ejemplo instantáneo',
    title: 'Semana NSA / NSM',
    input: '5K 20:00 · 6 h/semana',
    days: [
      ['Mar', '3 x 10 min', '4:28/km'],
      ['Jue', '5 x 6 min', '4:15/km'],
      ['Sáb', '9 x 3 min', '4:04/km'],
      ['Dom', '75 min fácil', '5:35/km'],
    ],
  },
  ko: {
    eyebrow: '즉시 예시',
    title: 'NSA / NSM 주간',
    input: '5K 20:00 · 주 6시간',
    days: [
      ['화', '3 x 10분', '4:28/km'],
      ['목', '5 x 6분', '4:15/km'],
      ['토', '9 x 3분', '4:04/km'],
      ['일', '75분 이지', '5:35/km'],
    ],
  },
}[locale];

const landingText = {
  en: {
    faqTitle: 'FAQ',
    faqIntro: 'Quick answers about NSA, NSM, and the calculator.',
    relatedTitle: 'Choose the right guide',
    relatedIntro: 'More useful pages, depending on what you want to do next.',
    paceTitle: 'Training paces',
    paceIntro:
      'NSA / NSM paces calculated from your race time. Use the calculator above to confirm or fine-tune.',
    shortSubT: 'Short sub-T',
    mediumSubT: 'Medium sub-T',
    longSubT: 'Long sub-T',
    easyPace: 'Easy pace',
    rep3: '3-min reps',
    rep6: '6-min reps',
    rep10: '10-min reps',
    easyRunning: 'easy running',
    lookupTitle: 'NSA training paces by race time',
    lookupIntro:
      'Select your race time to see the calculated sub-threshold training paces.',
  },
  es: {
    faqTitle: 'Preguntas frecuentes',
    faqIntro: 'Respuestas rápidas sobre NSA, NSM y la calculadora.',
    relatedTitle: 'Elige la guía adecuada',
    relatedIntro: 'Más páginas útiles según lo que quieras hacer ahora.',
    paceTitle: 'Ritmos de entrenamiento',
    paceIntro:
      'Ritmos NSA/NSM calculados desde tu marca. Úsalos como referencia y ajusta con la calculadora.',
    shortSubT: 'Sub-T corto',
    mediumSubT: 'Sub-T medio',
    longSubT: 'Sub-T largo',
    easyPace: 'Ritmo fácil',
    rep3: 'rep. 3 min',
    rep6: 'rep. 6 min',
    rep10: 'rep. 10 min',
    easyRunning: 'rodaje suave',
    lookupTitle: 'Ritmos NSA por marca de carrera',
    lookupIntro:
      'Selecciona tu marca para ver los ritmos sub-umbral calculados.',
  },
  ko: {
    faqTitle: '자주 묻는 질문',
    faqIntro: 'NSA, NSM, 이 독립 계산기 사용에 대한 빠른 답변입니다.',
    relatedTitle: '목적에 맞는 가이드 선택',
    relatedIntro: '지금 필요한 목적에 따라 이어서 볼 수 있는 페이지입니다.',
    paceTitle: '훈련 페이스',
    paceIntro:
      '레이스 기록으로 계산한 NSA/NSM 페이스입니다. 위 계산기로 확인하거나 조정하세요.',
    shortSubT: '짧은 Sub-T',
    mediumSubT: '중간 Sub-T',
    longSubT: '긴 Sub-T',
    easyPace: '이지 페이스',
    rep3: '3분 반복',
    rep6: '6분 반복',
    rep10: '10분 반복',
    easyRunning: '이지런',
    lookupTitle: '레이스 기록별 NSA 훈련 페이스',
    lookupIntro: '기록을 선택해 계산된 서브스레숄드 훈련 페이스를 확인하세요.',
  },
}[locale];

const educationLinks = {
  en: {
    nsm: '/en/nsm-calculator/#calculator',
    method: '/en/norwegian-method-calculator/#guide',
    bakken: '/en/marius-bakken-pace-calculator/#guide',
    marathon: '/en/norwegian-method-marathon-plan/#guide',
  },
  es: {
    nsm: '/es/calculadora-nsm/#calculator',
    method: '/es/calculadora-metodo-noruego/#guide',
    bakken: '/es/calculadora-ritmos-marius-bakken/#guide',
    marathon: '/es/plan-maraton-metodo-noruego/#guide',
  },
  ko: {
    nsm: '/ko/nsm-calculator/#calculator',
    method: '/ko/norwegian-method-calculator/#guide',
    bakken: '/ko/marius-bakken-pace-calculator/#guide',
    marathon: '/ko/norwegian-method-marathon-plan/#guide',
  },
}[locale];

const educationText = {
  en: {
    title: 'How to use these paces',
    intro:
      'The calculator gives conservative targets for weeks you can repeat.',
    cards: [
      {
        title: '3-minute reps',
        text: 'Use these for the shortest sub-threshold workout of the week. Keep them controlled and quick.',
      },
      {
        title: '6-minute reps',
        text: 'Use these as the middle session. They sit steadier than the 3-minute reps.',
      },
      {
        title: '10-minute reps',
        text: 'Use these for longer aerobic pressure. Finish the final rep with room left.',
      },
      {
        title: 'Easy pace',
        text: 'Easy runs stay easy enough that you recover for the next workout. If fatigue builds, slow them down first.',
      },
    ],
    nextTitle: 'Next step',
    actions: [
      {
        href: educationLinks.nsm,
        label: 'NSM calculator',
        eyebrow: 'Start here',
        text: 'Enter a race result and get 3, 6, and 10-minute paces.',
      },
      {
        href: educationLinks.method,
        label: 'Norwegian method calculator',
        eyebrow: 'Compare',
        text: 'See how single-session training fits Norwegian threshold ideas.',
      },
      {
        href: educationLinks.bakken,
        label: 'Marius Bakken pace notes',
        eyebrow: 'Background',
        text: 'Connect the pace targets with Bakken-style threshold training.',
      },
      {
        href: educationLinks.marathon,
        label: 'Norwegian method marathon plan',
        eyebrow: 'Marathon',
        text: 'Build a 15-week marathon block around sub-threshold days.',
      },
    ],
  },
  es: {
    title: 'Cómo usar estos ritmos',
    intro: 'La calculadora da ritmos conservadores para semanas repetibles.',
    cards: [
      {
        title: 'Repeticiones de 3 min',
        text: 'Úsalas para la sesión sub-umbral más corta de la semana. Corre rápido, pero con control.',
      },
      {
        title: 'Repeticiones de 6 min',
        text: 'Úsalas como sesión intermedia. Son más estables que las de 3 minutos.',
      },
      {
        title: 'Repeticiones de 10 min',
        text: 'Úsalas para presión aeróbica más larga. Termina la última repetición con margen.',
      },
      {
        title: 'Ritmo fácil',
        text: 'El rodaje fácil tiene que dejarte recuperar para la siguiente sesión. Si acumulas fatiga, baja este ritmo primero.',
      },
    ],
    nextTitle: 'Siguiente paso',
    actions: [
      {
        href: educationLinks.nsm,
        label: 'Calculadora NSM',
        eyebrow: 'Empieza aquí',
        text: 'Introduce una marca y obtén ritmos para 3, 6 y 10 minutos.',
      },
      {
        href: educationLinks.method,
        label: 'Calculadora método noruego',
        eyebrow: 'Compara',
        text: 'Conecta el método noruego con una semana de sesión única.',
      },
      {
        href: educationLinks.bakken,
        label: 'Ritmos Marius Bakken',
        eyebrow: 'Contexto',
        text: 'Relaciona estos ritmos con el trabajo de umbral de Bakken.',
      },
      {
        href: educationLinks.marathon,
        label: 'Plan maratón método noruego',
        eyebrow: 'Maratón',
        text: 'Monta un bloque de 15 semanas con días sub-umbral.',
      },
    ],
  },
  ko: {
    title: '이 페이스를 사용하는 방법',
    intro:
      '계산기는 전력 인터벌 목표가 아니라 반복 가능한 주간을 위한 보수적인 훈련 목표를 제공합니다.',
    cards: [
      {
        title: '3분 반복',
        text: '주간에서 가장 짧은 서브스레숄드 세션에 사용합니다. 빠르지만 통제 가능하고 반복 가능해야 합니다.',
      },
      {
        title: '6분 반복',
        text: '중간 세션에 사용합니다. 3분 반복보다 안정적이지만 여전히 임계값 아래에 있어야 합니다.',
      },
      {
        title: '10분 반복',
        text: '더 긴 유산소 압박을 주는 세션입니다. 마지막 반복이 버티기 모드가 되지 않을 정도로 여유가 있어야 합니다.',
      },
      {
        title: '이지 페이스',
        text: '이지런은 다음 운동을 회복할 수 있을 만큼 쉬워야 합니다. 피로가 쌓이면 이 페이스부터 늦추세요.',
      },
    ],
    nextTitle: '다음 단계',
    actions: [
      {
        href: educationLinks.nsm,
        label: 'NSM 계산기',
        eyebrow: '처음 시작',
        text: '페이스와 주간 구조만 빠르게 확인할 때 사용하세요.',
      },
      {
        href: educationLinks.method,
        label: '노르웨이식 훈련 계산기',
        eyebrow: '용어 비교',
        text: '노르웨이식 훈련이나 임계값 훈련으로 검색했다면 이 페이지가 맞습니다.',
      },
      {
        href: educationLinks.bakken,
        label: 'Marius Bakken 페이스 노트',
        eyebrow: '배경',
        text: '서브스레숄드 페이스의 노르웨이식 맥락을 볼 때 사용하세요.',
      },
      {
        href: educationLinks.marathon,
        label: '노르웨이식 마라톤 플랜',
        eyebrow: '마라톤',
        text: '목표가 주간 템플릿이 아니라 마라톤 빌드업일 때 사용하세요.',
      },
    ],
  },
}[locale];

const nextStepActions = educationText.actions.filter(
  (action) => !isCurrentHref(action.href),
);

const relatedGroups = {
  en: [
    {
      title: 'Pace calculators',
      links: [
        {
          slug: 'nsm-calculator',
          text: 'Race result in, 3/6/10-minute paces out.',
        },
        {
          slug: 'sub-threshold-pace-calculator',
          text: 'Use this when you care about threshold control.',
        },
        {
          slug: 'marius-bakken-pace-calculator',
          text: 'Bakken-style context for the pace targets.',
        },
      ],
    },
    {
      title: 'Plans',
      links: [
        {
          slug: 'norwegian-singles-training-plan',
          text: 'Turn the paces into a weekly structure.',
        },
        {
          slug: 'norwegian-method-marathon-plan',
          text: 'Build the same work into a marathon block.',
        },
      ],
    },
    {
      title: 'Comparisons',
      links: [
        {
          slug: 'norwegian-method-recreational-runners',
          text: 'Adapt Norwegian threshold work to normal training weeks.',
        },
        {
          slug: 'subthreshold-calculator-alternative',
          text: 'Compare pace tools without creating an account.',
        },
        {
          slug: 'norwegian-singles-calculator-comparison',
          text: 'See which planner features matter before you choose one.',
        },
      ],
    },
  ],
  es: [
    {
      title: 'Calculadoras de ritmos',
      links: [
        {
          slug: 'calculadora-nsm',
          text: 'Marca de carrera dentro, ritmos de 3/6/10 minutos fuera.',
        },
        {
          slug: 'calculadora-ritmos-sub-umbral',
          text: 'Úsala si quieres controlar mejor la intensidad.',
        },
        {
          slug: 'calculadora-ritmos-marius-bakken',
          text: 'Contexto Bakken para entender de dónde salen los ritmos.',
        },
      ],
    },
    {
      title: 'Planes',
      links: [
        {
          slug: 'plan-entrenamiento-norwegian-singles',
          text: 'Convierte los ritmos en una semana de entrenamiento.',
        },
        {
          slug: 'plan-maraton-metodo-noruego',
          text: 'Lleva el trabajo sub-umbral a un bloque de maratón.',
        },
      ],
    },
    {
      title: 'Comparativas',
      links: [
        {
          slug: 'metodo-noruego-corredores-populares',
          text: 'Adapta el umbral noruego a semanas normales.',
        },
        {
          slug: 'alternativa-calculadora-subthreshold',
          text: 'Compara herramientas de ritmo sin crear una cuenta.',
        },
        {
          slug: 'comparativa-calculadora-norwegian-singles',
          text: 'Revisa qué funciones importan antes de elegir planner.',
        },
      ],
    },
  ],
  ko: [
    {
      title: '페이스 계산기',
      links: [
        {
          slug: 'nsm-calculator',
          text: '레이스 기록으로 3/6/10분 페이스를 확인하세요.',
        },
        {
          slug: 'sub-threshold-pace-calculator',
          text: '운동 강도를 더 세밀하게 맞출 때 사용하세요.',
        },
        {
          slug: 'marius-bakken-pace-calculator',
          text: 'Bakken식 임계값 훈련 맥락을 확인하세요.',
        },
      ],
    },
    {
      title: '훈련 플랜',
      links: [
        {
          slug: 'norwegian-singles-training-plan',
          text: '계산한 페이스를 주간 훈련으로 바꾸세요.',
        },
        {
          slug: 'norwegian-method-marathon-plan',
          text: '서브스레숄드 훈련을 마라톤 블록에 넣어 보세요.',
        },
      ],
    },
    {
      title: '비교와 배경',
      links: [
        {
          slug: 'norwegian-method-recreational-runners',
          text: '노르웨이식 임계값 훈련을 일반 주간에 맞춰 보세요.',
        },
        {
          slug: 'subthreshold-calculator-alternative',
          text: '계정 없이 쓸 수 있는 페이스 도구를 비교하세요.',
        },
        {
          slug: 'norwegian-singles-calculator-comparison',
          text: '플래너를 고르기 전에 필요한 기능을 확인하세요.',
        },
      ],
    },
  ],
}[locale]
  .map((group) => ({
    ...group,
    pages: group.links
      .map((link) => {
        const page = relatedPages.find(
          (candidate) => candidate.slug === link.slug,
        );
        if (!page || isCurrentHref(`/${locale}/${page.slug}/`)) {
          return undefined;
        }
        return { ...page, summary: link.text };
      })
      .filter(
        (page): page is (typeof relatedPages)[number] & { summary: string } =>
          Boolean(page),
      ),
  }))
  .filter((group) => group.pages.length > 0);
---

<AppLayout
  title={title || t('seo.title')}
  description={description || t('seo.description')}
  locale={locale}
  structuredData={[...(structuredData || []), faqStructuredData]}
>
  <div class="landing">
    <section class="hero" id="calculator">
      <div class="container hero-grid">
        <div class="hero-content">
          <p class="hero-kicker">Norwegian Singles Approach / Method</p>
          {
            isSeoLanding ? (
              <h1 class="hero-title">{seoHeading}</h1>
            ) : (
              <h1 class="hero-title">
                {t('hero.title')}
                <span class="hero-accent">{t('hero.titleAccent')}</span>
              </h1>
            )
          }
          {
            isSeoLanding && seoIntro ? (
              <p class="hero-subtitle" set:html={seoIntro} />
            ) : (
              <p class="hero-subtitle">{t('hero.subtitle')}</p>
            )
          }

          <ul class="proof-list" aria-label="Calculator features">
            {proofBullets.map((item) => <li>{item}</li>)}
          </ul>

          <div
            class="sample-board sample-board-desktop"
            aria-label={sample.eyebrow}
          >
            <div class="sample-head">
              <span>{sample.eyebrow}</span>
              <strong>{sample.title}</strong>
              <em>{sample.input}</em>
            </div>
            <div class="sample-days">
              {
                sample.days.map(([day, session, pace]) => (
                  <div class="sample-day">
                    <span>{day}</span>
                    <strong>{session}</strong>
                    <em>{pace}</em>
                  </div>
                ))
              }
            </div>
          </div>
        </div>

        <div class="hero-product">
          <div class="form-card card glass">
            <div class="card-header">
              <h2 class="card-title">{t('form.title')}</h2>
            </div>
            <div class="card-body">
              <TrainingForm locale={locale} client:idle />
            </div>
          </div>
        </div>

        <div
          class="sample-board sample-board-mobile"
          aria-label={sample.eyebrow}
        >
          <div class="sample-head">
            <span>{sample.eyebrow}</span>
            <strong>{sample.title}</strong>
            <em>{sample.input}</em>
          </div>
          <div class="sample-days">
            {
              sample.days.map(([day, session, pace]) => (
                <div class="sample-day">
                  <span>{day}</span>
                  <strong>{session}</strong>
                  <em>{pace}</em>
                </div>
              ))
            }
          </div>
        </div>
      </div>
    </section>

    <section class="education-section section">
      <div class="container education-container">
        <div class="education-head">
          <h2>{educationText.title}</h2>
          <p>{educationText.intro}</p>
        </div>

        <div class="education-grid">
          {
            educationText.cards.map((card) => (
              <article class="education-card">
                <h3>{card.title}</h3>
                <p>{card.text}</p>
              </article>
            ))
          }
        </div>

        <div class="next-step-panel">
          <h3>{educationText.nextTitle}</h3>
          <div class="next-step-grid">
            {
              nextStepActions.map((action) => (
                <a class="next-step-link" href={action.href}>
                  <span>{action.eyebrow}</span>
                  <strong>{action.label}</strong>
                  <em>{action.text}</em>
                </a>
              ))
            }
          </div>
        </div>
      </div>
    </section>

    <MethodologySection locale={locale} />

    <BookRecommendations locale={locale} />

    {
      paceCard && (
        <section class="pace-card-section section">
          <div class="container pace-card-container">
            <div class="pace-card-head">
              <h2>{`${landingText.paceTitle}: ${paceCard.raceLabel}`}</h2>
              <p>{landingText.paceIntro}</p>
            </div>
            <div class="pace-grid">
              <div class="pace-cell">
                <span>{landingText.shortSubT}</span>
                <strong>{paceCard.rep3min}</strong>
                <em>{landingText.rep3}</em>
              </div>
              <div class="pace-cell">
                <span>{landingText.mediumSubT}</span>
                <strong>{paceCard.rep6min}</strong>
                <em>{landingText.rep6}</em>
              </div>
              <div class="pace-cell">
                <span>{landingText.longSubT}</span>
                <strong>{paceCard.rep10min}</strong>
                <em>{landingText.rep10}</em>
              </div>
              <div class="pace-cell">
                <span>{landingText.easyPace}</span>
                <strong>{paceCard.easy}</strong>
                <em>{landingText.easyRunning}</em>
              </div>
            </div>
          </div>
        </section>
      )
    }

    <section class="seo-content section" id="guide">
      <div class="container seo-container">
        <h2>{seoHeading || t('seo.content.h2')}</h2>
        {
          seoIntro ? (
            <p set:html={seoIntro} />
          ) : (
            <p set:html={t('seo.content.intro')} />
          )
        }

        <h3>{t('seo.content.doubleThreshold.title')}</h3>
        <p set:html={t('seo.content.doubleThreshold.text')} />

        <h3>{t('seo.content.howItWorks.title')}</h3>
        <p set:html={t('seo.content.howItWorks.text')} />
        <ul>
          <li set:html={t('seo.content.howItWorks.rep3')} />
          <li set:html={t('seo.content.howItWorks.rep6')} />
          <li set:html={t('seo.content.howItWorks.rep10')} />
        </ul>
        <p set:html={t('seo.content.howItWorks.paces')} />

        <h3>{t('seo.content.weeklyPlans.title')}</h3>
        <p set:html={t('seo.content.weeklyPlans.text')} />

        <h3>{t('seo.content.marathon.title')}</h3>
        <p set:html={t('seo.content.marathon.text')} />

        <h3>{t('seo.content.whoIsItFor.title')}</h3>
        <p set:html={t('seo.content.whoIsItFor.text')} />
      </div>
    </section>

    <section class="faq-section section">
      <div class="container">
        <div class="section-head">
          <h2>{landingText.faqTitle}</h2>
          <p>{landingText.faqIntro}</p>
        </div>

        <div class="faq-grid">
          {
            faqs.map((faq) => (
              <article class="faq-item">
                <h3>{faq.q}</h3>
                <p>{faq.a}</p>
              </article>
            ))
          }
        </div>
      </div>
    </section>

    <section class="related-section section">
      <div class="container">
        <div class="section-head">
          <h2>
            {landingText.relatedTitle}
          </h2>
          <p>{landingText.relatedIntro}</p>
        </div>

        <div class="related-groups">
          {
            relatedGroups.map((group) => (
              <article class="related-group">
                <h3>{group.title}</h3>
                <div class="related-grid">
                  {group.pages.map((page) => (
                    <article class="related-link">
                      <h4>
                        <a href={`/${locale}/${page.slug}/`}>{page.h1}</a>
                      </h4>
                      <p>{page.summary}</p>
                    </article>
                  ))}
                </div>
              </article>
            ))
          }
        </div>
      </div>
    </section>

    <section class="pace-lookup-section section">
      <div class="container">
        <div class="section-head">
          <h2>{landingText.lookupTitle}</h2>
          <p>{landingText.lookupIntro}</p>
        </div>
        <div class="pace-lookup-grid">
          {
            raceGroups.map(({ key, label }) => (
              <div class="pace-lookup-group">
                <h3 class="pace-lookup-label">{label}</h3>
                <div class="pace-lookup-times">
                  {racePagesByDistance[key].map((page) => (
                    <a
                      class="pace-lookup-time"
                      href={`/${locale}/${page.slug}/`}
                    >
                      {page.displayTime}
                    </a>
                  ))}
                </div>
              </div>
            ))
          }
        </div>
      </div>
    </section>
  </div>
</AppLayout>

<style>
  .landing {
    background:
      linear-gradient(180deg, rgba(59, 130, 246, 0.12), transparent 30rem),
      var(--color-bg-primary);
  }

  .hero {
    min-height: calc(100vh - var(--header-height));
    display: flex;
    align-items: center;
    padding: var(--space-10) 0 var(--space-16);
  }

  .hero-grid {
    display: grid;
    gap: var(--space-10);
  }

  @media (min-width: 1080px) {
    .hero-grid {
      grid-template-columns: minmax(0, 1fr) minmax(430px, 520px);
    }
  }

  .hero-content {
    text-align: left;
  }

  .hero-kicker {
    display: inline-flex;
    margin-bottom: var(--space-4);
    padding: var(--space-2) var(--space-3);
    color: var(--color-text-primary);
    background: rgba(59, 130, 246, 0.12);
    border: 1px solid rgba(59, 130, 246, 0.28);
    border-radius: var(--radius-full);
    font-size: var(--text-xs);
    font-weight: var(--font-bold);
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  .hero-title {
    max-width: 760px;
    font-size: var(--text-4xl);
    line-height: 1.05;
    margin-bottom: var(--space-4);
    letter-spacing: 0;
  }

  @media (min-width: 768px) {
    .hero-title {
      font-size: var(--text-6xl);
    }
  }

  .hero-accent {
    display: block;
    background: linear-gradient(135deg, #f97316 0%, #ef4444 45%, #60a5fa 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-subtitle {
    font-size: var(--text-lg);
    color: var(--color-text-secondary);
    max-width: 650px;
    margin: 0;
  }

  .proof-list {
    display: grid;
    gap: var(--space-2);
    margin: var(--space-6) 0;
    padding: 0;
    list-style: none;
    li {
      display: flex;
      align-items: center;
      justify-content: flex-start;
    }
  }

  @media (min-width: 680px) {
    .proof-list {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      max-width: 640px;
    }
  }

  .proof-list li {
    position: relative;
    padding: var(--space-3) var(--space-4) var(--space-3) 2.4rem;
    color: var(--color-text-primary);
    background: rgba(255, 255, 255, 0.045);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    font-size: var(--text-sm);
    font-weight: var(--font-semibold);
  }

  .proof-list li::before {
    content: '';
    position: absolute;
    left: var(--space-4);
    top: 50%;
    width: 0.55rem;
    height: 0.55rem;
    background: #22c55e;
    border-radius: 50%;
    transform: translateY(-50%);
    box-shadow: 0 0 0 4px rgba(34, 197, 94, 0.14);
  }

  .sample-board {
    max-width: 680px;
    padding: var(--space-5);
    background: #111113;
    border: 1px solid var(--color-border);
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-xl);
  }

  .sample-board-desktop {
    display: none;
  }

  @media (max-width: 1079px) {
    .sample-board-mobile {
      display: block;
    }
  }

  @media (min-width: 1080px) {
    .sample-board-desktop {
      display: block;
    }

    .sample-board-mobile {
      display: none;
    }
  }

  .sample-head {
    display: grid;
    gap: var(--space-1);
    margin-bottom: var(--space-4);
  }

  .sample-head span,
  .sample-head em {
    color: var(--color-text-muted);
    font-size: var(--text-xs);
    font-style: normal;
  }

  .sample-head span {
    font-weight: var(--font-bold);
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .sample-head strong {
    font-size: var(--text-xl);
  }

  .sample-days {
    display: grid;
    gap: var(--space-2);
  }

  @media (min-width: 620px) {
    .sample-days {
      grid-template-columns: repeat(4, minmax(0, 1fr));
    }
  }

  .sample-day {
    min-height: 7rem;
    display: flex;
    flex-direction: column;
    justify-content: space-between;
    padding: var(--space-3);
    background: rgba(255, 255, 255, 0.04);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-md);
  }

  .sample-day span {
    color: var(--color-text-muted);
    font-size: var(--text-xs);
    font-weight: var(--font-bold);
    text-transform: uppercase;
  }

  .sample-day strong {
    color: var(--color-text-primary);
    font-size: var(--text-sm);
  }

  .sample-day em {
    color: var(--color-threshold);
    font-family: var(--font-mono);
    font-size: var(--text-sm);
    font-style: normal;
    font-weight: var(--font-bold);
  }

  .hero-product {
    width: 100%;
  }

  @media (min-width: 1080px) {
    .hero-product {
      grid-column: 2;
    }
  }

  .form-card {
    max-width: 520px;
    margin: 0 auto;
    box-shadow: var(--shadow-xl);
  }

  @media (min-width: 1080px) {
    .form-card {
      margin: 0 0 0 auto;
    }
  }

  .pace-card-section {
    padding: var(--space-16) 0;
    border-top: 1px solid var(--color-border);
  }

  .pace-card-container {
    max-width: 900px;
  }

  .pace-card-head {
    max-width: 640px;
    margin-bottom: var(--space-8);
  }

  .pace-card-head h2 {
    font-size: var(--text-3xl);
    margin-bottom: var(--space-3);
  }

  .pace-card-head p {
    color: var(--color-text-secondary);
    font-size: var(--text-base);
    line-height: 1.65;
    margin: 0;
  }

  .pace-grid {
    display: grid;
    gap: var(--space-3);
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }

  @media (min-width: 640px) {
    .pace-grid {
      grid-template-columns: repeat(4, minmax(0, 1fr));
    }
  }

  .pace-cell {
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
    padding: var(--space-5);
    background: rgba(255, 255, 255, 0.04);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
  }

  .pace-cell span {
    color: var(--color-text-muted);
    font-size: var(--text-xs);
    font-weight: var(--font-bold);
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }

  .pace-cell strong {
    color: var(--color-threshold);
    font-family: var(--font-mono);
    font-size: var(--text-2xl);
    font-weight: var(--font-bold);
    line-height: 1;
  }

  .pace-cell em {
    color: var(--color-text-secondary);
    font-size: var(--text-xs);
    font-style: normal;
  }

  .seo-content {
    padding: var(--space-16) 0;
    border-top: 1px solid var(--color-border);
  }

  .seo-container {
    max-width: 780px;
  }

  .seo-content h2 {
    font-size: var(--text-3xl);
    margin-bottom: var(--space-6);
  }

  .seo-content h3 {
    font-size: var(--text-xl);
    margin-top: var(--space-8);
    margin-bottom: var(--space-3);
  }

  .seo-content p {
    font-size: var(--text-base);
    color: var(--color-text-secondary);
    line-height: 1.75;
    margin-bottom: var(--space-4);
  }

  .seo-content ul {
    list-style: none;
    padding: 0;
    margin: 0 0 var(--space-4) 0;
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
  }

  .seo-content li {
    font-size: var(--text-base);
    color: var(--color-text-secondary);
    line-height: 1.75;
    padding-left: var(--space-5);
    position: relative;
  }

  .seo-content li::before {
    content: '>';
    position: absolute;
    left: 0;
    color: var(--color-accent-primary);
  }

  .seo-content strong {
    color: var(--color-text-primary);
    font-weight: var(--font-semibold);
  }

  .seo-content em {
    color: var(--color-text-primary);
  }

  .faq-section,
  .related-section {
    border-top: 1px solid var(--color-border);
  }

  .education-section {
    border-top: 1px solid var(--color-border);
    background: rgba(255, 255, 255, 0.018);
  }

  .education-container {
    max-width: 1080px;
  }

  .education-head {
    max-width: 760px;
    margin-bottom: var(--space-8);
  }

  .education-head h2 {
    margin-bottom: var(--space-3);
  }

  .education-head p {
    margin: 0;
    color: var(--color-text-secondary);
  }

  .education-grid {
    display: grid;
    gap: var(--space-3);
  }

  @media (min-width: 760px) {
    .education-grid {
      grid-template-columns: repeat(4, minmax(0, 1fr));
    }
  }

  .education-card {
    min-height: 11rem;
    padding: var(--space-5);
    background: rgba(255, 255, 255, 0.035);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
  }

  .education-card h3 {
    margin-bottom: var(--space-2);
    font-size: var(--text-base);
  }

  .education-card p {
    margin: 0;
    color: var(--color-text-secondary);
    font-size: var(--text-sm);
    line-height: 1.55;
  }

  .next-step-panel {
    margin-top: var(--space-8);
  }

  .next-step-panel h3 {
    margin-bottom: var(--space-3);
    color: var(--color-text-muted);
    font-size: var(--text-sm);
    font-weight: var(--font-bold);
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }

  .next-step-grid {
    display: grid;
    gap: var(--space-3);
  }

  @media (min-width: 760px) {
    .next-step-grid {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }
  }

  .next-step-link {
    display: grid;
    gap: var(--space-2);
    min-height: 10rem;
    padding: var(--space-5);
    color: var(--color-text-primary);
    background: rgba(17, 17, 19, 0.68);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    text-decoration: none;
    transition:
      background var(--transition-fast),
      border-color var(--transition-fast),
      transform var(--transition-fast);
  }

  .next-step-link:hover {
    background: rgba(239, 68, 68, 0.08);
    border-color: rgba(239, 68, 68, 0.36);
    transform: translateY(-2px);
  }

  .next-step-link span,
  .next-step-link em {
    color: var(--color-text-muted);
    font-size: var(--text-xs);
    font-style: normal;
  }

  .next-step-link span {
    font-weight: var(--font-bold);
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }

  .next-step-link strong {
    font-size: var(--text-base);
    line-height: 1.25;
  }

  .next-step-link em {
    color: var(--color-text-secondary);
    line-height: 1.45;
  }

  .section-head {
    max-width: 720px;
    margin: 0 auto var(--space-8);
    text-align: center;
  }

  .section-head h2 {
    margin-bottom: var(--space-3);
  }

  .section-head p {
    margin: 0;
  }

  .faq-grid {
    display: grid;
    gap: var(--space-4);
    max-width: 980px;
    margin: 0 auto;
  }

  @media (min-width: 760px) {
    .faq-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  .faq-item {
    padding: var(--space-5);
    background: var(--color-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
  }

  .faq-item h3 {
    font-size: var(--text-lg);
    margin-bottom: var(--space-2);
  }

  .faq-item p {
    margin: 0;
    color: var(--color-text-secondary);
  }

  .related-groups {
    display: grid;
    gap: var(--space-12);
    max-width: 1080px;
    margin: 0 auto;
  }

  .related-group {
    padding-top: var(--space-8);
    border-top: 1px solid var(--color-border);
  }

  .related-group:first-child {
    padding-top: 0;
    border-top: 0;
  }

  .related-group h3 {
    margin-bottom: var(--space-5);
    color: var(--color-text-muted);
    font-size: var(--text-sm);
    font-weight: var(--font-bold);
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }

  .related-grid {
    display: grid;
    gap: var(--space-3);
  }

  @media (min-width: 760px) {
    .related-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  @media (min-width: 1120px) {
    .related-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  .related-link {
    min-height: 7rem;
    padding: var(--space-4);
    background: rgba(255, 255, 255, 0.035);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-lg);
    transition:
      background var(--transition-fast),
      border-color var(--transition-fast);
  }

  .related-link:hover {
    background: rgba(255, 255, 255, 0.055);
    border-color: var(--color-border-accent);
  }

  .related-link h4 {
    margin: 0 0 var(--space-2);
    font-size: var(--text-base);
    line-height: 1.3;
  }

  .related-link a {
    color: var(--color-text-primary);
    text-decoration: none;
  }

  .related-link a:hover {
    color: var(--color-accent-primary);
  }

  .related-link p {
    margin: 0;
    color: var(--color-text-secondary);
    font-size: var(--text-sm);
    line-height: 1.5;
  }

  .pace-lookup-section {
    border-top: 1px solid var(--color-border);
  }

  .pace-lookup-grid {
    display: grid;
    gap: var(--space-8);
    grid-template-columns: repeat(2, minmax(0, 1fr));
    max-width: 900px;
    margin: 0 auto;
  }

  @media (min-width: 760px) {
    .pace-lookup-grid {
      grid-template-columns: repeat(4, minmax(0, 1fr));
    }
  }

  .pace-lookup-label {
    font-size: var(--text-sm);
    font-weight: var(--font-bold);
    letter-spacing: 0.06em;
    text-transform: uppercase;
    color: var(--color-text-muted);
    margin-bottom: var(--space-3);
  }

  .pace-lookup-times {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2);
  }

  .pace-lookup-time {
    display: inline-block;
    padding: var(--space-1) var(--space-3);
    color: var(--color-text-secondary);
    background: rgba(255, 255, 255, 0.045);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-full);
    font-family: var(--font-mono);
    font-size: var(--text-sm);
    text-decoration: none;
    transition:
      background var(--transition-fast),
      border-color var(--transition-fast),
      color var(--transition-fast);
  }

  .pace-lookup-time:hover {
    color: var(--color-text-primary);
    background: rgba(255, 255, 255, 0.08);
    border-color: var(--color-border-accent);
  }
</style>
